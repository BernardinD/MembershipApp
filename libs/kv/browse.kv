<FileBrowser>:
	size_hint: 0.75, 0.75
	FileChooserListView:
		# Grab topic name from popup and save selection to that key
		#on_selection: root.root.dismiss(self.selection)
		on_selection: root.selection = self.selection[0]
		
<Browser>:
	color: 0, 0, 0, 1
	canvas.before:
		Color:
			rgba: 1,1,1,1
		Rectangle:
			size: self.size
			pos: self.pos