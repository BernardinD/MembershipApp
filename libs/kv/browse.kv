# Popup for selecting file
<Browser>:
	size_hint: 0.75, 0.75
	FileChooserListView:
		# Grab topic name from popup and save selection to that key
		#on_selection: root.root.dismiss(self.selection)
		on_selection: root.selection = self.selection[0] if len(self.selection) > 0 else ""
		